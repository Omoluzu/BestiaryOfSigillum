
### Некоторые заметка для улучшения в следующих играх.

##### 1
В классе [[Scene]] Явно не создавать экземпляры классов наследованные от [[ElementScene]].  
Данное желание обусловлено тем что при инициализации [[ElementScene]] отрисовывается сразу же. И в [[wrapperQWidget5/Documentation/GamesModules/Scene|Scene]] приходится либо не использовать инициализацию, или не использовать draw.  
Ну или проработать возможность отложенной отрисовки. с явным методом draw


##### 2
Создать группу которая объединяет в себе некоторые [[ElementScene]], Группа будет отвечать за общую центральную точку, то есть все остальные элементы считают основной точкой данную группу а не сцену, и позиционируются уже относительно её, так же если группу имеет параметр rotate то элемент поворачивается относительно центра группы.

Тут же все [[ElementScene]] получает дополнительный параметр что то типа `parent` (Занят QT) для понятия какой у элемента родитель.

##### 3
Дополнительные надстройки над [[ElementScene]] явно указывающий какой тип элемента. На текущий момент их 2.  
`Tile`(`Card`/`Mipl`). Элементы которые могут активироваться по умолчанию и выставляться на тип элемента `Field`.  
`Field`(`Map`). Элемент которые не активируется игроков, но отсылает команду на сервер при выставлении на него `Tile`.

##### 4
Вывести в отдельный класс элемент отрисовки текста с игровым ником для понимаю очередности хода. Сейчас я уже второй раз для AZUL сделал кастомные виджеты для отрисовки Стрелки указывающей на того чей сейчас ход и имени. Так же нужно было бы сделать подсчет очков наверное тут же.   
Класс может иметь следующие стандартные методы: `selected`, `unselected`(`deactivate`), `set_points`, `get_points`, `__bool__`

##### 5
Вывести порядок хода игроков и инициализировать только разово при запуски игры. Сейчас эта информация хранится в сайвах игры.
Сделать какой ни-будь механизм(Отдельный класс который отвечает за это)

##### 6
При инициализации [[ElementScene]] сохранять свои данные в какой ни-будь словарик с определенным именем. Это нужно для того чтобы не искать элемент по его структуре, а найти его по его ключу в этом словаре. Собственно необходимо добавить обязательный параметр key,name,id в [[ElementScene]], ну или сделать его не обязательным и при его отсутвии в данный словарик экземпляр класса не добавляется.

##### 7
Хранить на сервере информацию о переданных данных о ходе игрока. 

##### 8 
Автоматизировать действия с `last_move`. 

##### 9
`Games.get_commands` Под капот вынести получение команды и автоматически вызывать нужный метод. Если метода нет выводить соответсвующую ошибку. Подумать над выводом дейстий из Scene в Games.

##### 10 (Top)
Избавится от сабмодуля WrapperQWidget5 и перевести работу на PySide6

##### 11 (Top)
Перевести клиент серверное взаимодействие на AMQUtils

##### 12 (Top)
Увеличение/Уменьшение размеров элементов поля.
 